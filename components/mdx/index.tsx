/**
 * Mapa de componentes para MDXRemote
 * Exporta todos los componentes MDX disponibles con sus tipos.
 */
import { Var } from './Var'
import { If } from './If'
import { Money } from './Money'
import { NumberToText } from './NumberToText'
import { DateEs } from './DateEs'
import { Signatures } from './Signatures'

export { Var, If, Money, NumberToText, DateEs, Signatures }

export const mdxComponents = {
  Var,
  If,
  Money,
  NumberToText,
  DateEs,
  Signatures,
}
